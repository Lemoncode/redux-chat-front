import { createStyles } from "@material-ui/core/styles";

export default () => createStyles({
  card: {
    width: '75%',
    maxWidth: '40rem',
    minWidth: '20rem',
  },
  cardContent: {
    marginTop: '1rem',
  },
  cardActions: {
    padding: '1rem 1.5rem',
  }
});
