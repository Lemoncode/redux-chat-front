import { createStyles } from "@material-ui/core/styles";

export default () => createStyles({
  nicknameInput: {
    flex: 1,
    marginTop: 0,
  },
  joinButton: {
    minWidth: '5rem',
    marginLeft: '1.5rem',
  }
});